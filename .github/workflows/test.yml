name: "test-test"
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo HELLO
      - run: echo $PR_NUMBER
      # - run: echo "${{ toJson(github.event) }}"
